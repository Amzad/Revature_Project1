@model Revature_Project1.Models.BusinessCheckingAccount

@{
    ViewBag.Title = "Deposit";
}

<h2></h2>

@using (Html.BeginForm("Deposit", "BusinessChecking", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Deposit to Business Checking Account</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.AccountID)
        @Html.HiddenFor(model => model.Credit)
        @Html.HiddenFor(model => model.Debit)

        <div class="form-group col-md-3">
            <label>Account ID</label>
            @Html.EditorFor(model => model.AccountID, new { htmlAttributes = new { @class = "form-control", disabled = "disabled", @readonly = "readonly" } })
        </div>

        <div class="form-group col-md-3">
            @Html.LabelFor(model => model.Credit, htmlAttributes: new { @class = "control-label" })
            @Html.EditorFor(model => model.Credit, new { htmlAttributes = new { @class = "form-control", disabled = "disabled", @readonly = "readonly" } })
        </div>

        <div class="form-group col-md-3">
            <label>Overdraft</label>
            @Html.EditorFor(model => model.Debit, new { htmlAttributes = new { @class = "form-control", disabled = "disabled", @readonly = "readonly" } })
        </div>

        <div class="form-group col-md-3">
            <label for="depositvalue">Deposit Amount</label>
                <input type="number" min="0" name="depositvalue" id="depositvalue" class="form-control" required><br>
        </div>

        <div class="form-group col-md-3">
             <button type="submit" value="Deposit" class="btn btn-primary">Deposit</button>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to Account List", "Index", "Home")
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
